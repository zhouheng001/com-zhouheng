package com.tujia.comjdk.stream;

/**
 * 描述: dfa
 *
 * @author zhouheng
 * @create 2018-10-28 下午 11:18
 */
public class Lix {

    public static void main(String[] args) {

        // List<String> s1 = Arrays.asList("123", "234", "345","4dffsd", "4dsfs", "sdf5","sdf6", "sdfsdf7");
        //
        // List<String[]> collect = s1.stream().map(s -> s.split("")).collect(Collectors.toList());
        //
        //// collect.forEach(System.out::println);
        //
        // List<Stream<String>> collect2 = s1.stream().map(s ->
        // s.split("")).map(Arrays::stream).distinct().collect(Collectors.toList());
        //
        //
        // List<String> collect1 = s1.stream().map(s ->
        // s.split("")).flatMap(Arrays::stream).distinct().collect(Collectors.toList());
        //
        // collect1.forEach(System.out::println);
        if ((6 & 3) == 2) {
            System.out.println(123);
        }

    }
}
